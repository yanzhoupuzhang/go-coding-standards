---
name: go-coding-standards
description: 编写、修改或审查 Go 代码时应用统一规范：结构简单性、复用、格式化、命名、导入、中文注释、错误处理、日志、接口设计、并发安全、测试和改动边界，并参考 Go 官方与业界风格指南。适用于 Go 源码、运行时配置、项目文档和测试；不适用于非 Go 项目或纯讨论不落地的咨询。
---

# Go 通用编码规范

在编写或修改任何 Go 代码、运行时配置、项目文档或测试前，先遵守本文件；需要架构评审、代码审查或外部规范细节时，按需读取 `references/architecture.md`、`references/code-review.md` 和 `references/external-standards.md`。

## 核心目标

生成最少且可运行的完整代码，遵循项目既有约定，不扩大改动范围，不使用省略号或占位符代替实现。

## 结构与简单性

- 调用层级不超过 2 层；不过度设计，业务逻辑就地写，不为单个使用点抽取辅助函数或构造器。
- 行数优先少，不追求抽象或优雅；`map` 当场写；错误当场处理。
- 同一业务逻辑优先集中在一个方法内实现；禁止为同一逻辑创建多个重复方法，或把同一逻辑散落在多个位置。只有职责边界清晰、确有复用或复杂度需要时才拆分，并保留唯一实现入口。
- 禁止把 `interface{}/any` 作为通用字段或参数类型做动态分派，禁止反射解析；字段和分支显式处理。已存在接口值上的明确类型断言属于可接受场景，但失败必须显式处理。
- 每个包保持单一职责，避免为“可能复用”提前抽象。

## 复用规则

- 公共包和工具函数统一放在项目当前的公共目录；公共目录位置遵循项目既有约定，不写死路径。
- 使用前先查公共目录；已有同功能函数时直接调用，不在业务包内重新实现。
- 同一逻辑在两个及以上包中重复时，提取到项目公共目录并删除副本；单处使用的逻辑不提取。

## 第三方调用规则

- 调用第三方 HTTP 接口，尤其是 `POST` 和 `GET`，统一放入按服务划分的公共客户端或公共方法中；统一处理 URL、请求头、认证、超时、序列化、状态码和错误。
- 业务方法和导出方法不得直接调用第三方客户端的 `Post`、`Get` 等传输方法，只调用公共封装或按服务提供的类型化方法，避免第三方调用散落各处。

## 格式与命名

- 提交前运行 `gofmt`；导入行用 `goimports` 自动整理。
- 标识符统一采用驼峰命名：导出名用大驼峰，非导出名用小驼峰；禁止用下划线作为标识符分隔。
- 变量名、函数名、参数名和类型名至少 4 个字符，必须有明确业务含义；禁止 `a`、`b`、`v`、`f` 这类过短命名。接收器名、标准惯例中的缩写和 `err` 可例外，例如 `c` 表示 `Client`、`ctx` 表示 `Context`、`err` 表示错误。
- 导入分组：标准库一组，外部依赖另一组，组间空行分隔。
- 包名使用简短小写单词，避免 `util`、`common`、`misc`、`api`、`types` 等无意义名称。
- 缩写词大小写保持一致，例如 `ServeHTTP` 而非 `ServeHttp`，`appID` 而非 `appId`。
- 接收器名用类型的一到两个字母缩写，例如 `c` 表示 `Client`；不使用 `me`、`this`、`self`。
- 获取器直接使用字段名，通常不加 `Get` 前缀。
- 避免使用内置名称，避免裸参数；局部变量作用域尽量小。
- 行不要过长；同一处代码风格保持前后一致。

## 导入

- 只有冲突时才重命名导入，优先重命名更局部或项目特有的导入。
- 空白导入 `_` 只用于主程序或测试中确实需要副作用的场景。
- 禁止点导入 `.`。

## 注释规则

- 注释使用中文；标识符和函数名除外。
- 每个方法使用多行块注释：`/*` 和 `*/` 必须各占一行，中间逐行说明干什么用、各参数作用、返回值；禁止写成 `/* 干什么 */` 这种单行简写。
- 方法内部用 `//`，只注释业务关键点，不逐行注释。

```go
/*
计算订单含税总价
参数：items 是订单明细，taxRate 是税率
返回：含税总价
*/
func Total(items []OrderItem, taxRate float64) float64 {
	// 这里只保留业务关键点
	sum := 0.0
	for _, item := range items {
		sum += item.Price
	}
	return sum * (1 + taxRate)
}
```

## 错误处理

- 错误字符串使用小写开头，不以标点结尾；例如 `"open failed"` 而非 `"Open failed."`。
- 不要用 `_` 丢弃错误；必须处理或返回错误，禁止用 `panic` 代替错误处理。
- 优先使用缩进错误流：先处理并返回错误，让正常路径保持最小缩进。
- 错误只包装一次并保留原始错误，避免重复记录同一错误。
- 不通过特殊返回值或哨兵值代替明确的 `error` 返回值。
- 类型断言失败必须显式处理，禁止静默忽略。

## 日志规则

- 日志库固定使用 `github.com/rs/zerolog`。
- 函数入口记录关键入参；密码、token 等敏感信息用 `"[REDACTED]"`。
- 每个 `return err` 前记录错误详情。
- 关键 `if/for/switch` 记录进入条件和结果。
- 只使用两个级别：`Error` 表示错误，`Info` 表示关键流程节点，禁止其他级别。
- 同一位置只打一条日志；打了 `Error` 就不再打 `Info`，反之亦然。
- 最小化插入日志，不改变原有业务逻辑。
- 日志输出位置遵循项目约定；默认写入日志文件，不输出控制台。

## 接口与类型
- 现代 Go 统一使用 `any`，不写 `interface{}`。

- 接口定义在使用方的包，不定义在实现方。
- 实现方返回具体类型，通常是结构体或指针。
- 不先定义接口再等待使用；也不仅为 mock 定义接口。
- 边界处拷贝切片和 map，避免调用方被内部数据修改影响。
- 避免在公开结构体中嵌入外部类型而暴露无关字段。
- 避免复制带有指针接收者方法的类型。

## 并发与安全

- 请求级函数把 `context.Context` 作为第一个参数显式传递；确非请求级时才使用 `context.Background()`。
- 不创建“发出后不管”的 goroutine；确保 goroutine 生命周期可退出、可等待。
- 不在 `init()` 中启动 goroutine；尽量减少 `init()` 使用。
- 密钥、token、随机数使用 `crypto/rand`，不使用 `math/rand`。
- 业务错误作为返回值处理，不使用 `panic`；仅程序初始化阶段遇到不可恢复错误时才可用 `panic` 或 `Must`。
- 时间用 `time.Time`，时间间隔用 `time.Duration`，不用裸整数。
- 通道缓冲大小为一或零；零值互斥锁可直接使用，禁止复制含锁结构。
- 避免可变全局变量。

## 性能

- 热点路径优先使用 `strconv` 而不是 `fmt` 做数字与字符串转换。
- 可预估容量时，为 map 和切片指定容量提示。
- 避免重复的字符串与字节切片互相转换。

## 测试规则

- 修改源代码时，必须在同一任务中新增或更新对应自动化测试；只改文档不要求新增测试。
- 所有 `_test.go` 和测试脚本放在项目约定的测试目录，业务源码目录内不放测试文件。
- 测试文件属于项目代码，验证完成后保留；只删除测试产生的日志、缓存、临时数据。
- 测试使用临时目录和独立测试数据，不修改项目内容资产，也不依赖已有内容资产才能通过。
- 需要测试私有逻辑时，迁移到职责明确、可导入的包并提供最小必要接口；禁止在测试中复制业务实现。
- 优先使用表驱动测试和子测试；失败信息包含函数、输入、期望值 `got` 与实际值 `want`。
- 测试公开 API 时，优先放在外部包以模拟真实调用方。
- 使用标准库 `testing`，不引入断言库。
- 修改完成后运行 `go test ./...`；测试失败不得报告任务完成。

## 改动边界与安全

- 只修改与当前请求相关的源代码、运行时配置、项目文档和测试。
- 不修改用户未指定的内容资产、生产数据或无关目录。
- 测试运行产生的副作用文件必须在结束后清理。
- 不做用户未要求的破坏性操作。

## 详细规则

- 架构与目录：`references/architecture.md`
- 代码审查：`references/code-review.md`
- 外部规范对照：`references/external-standards.md`

## 网络参考

本规范优先；外部指南仅作补充：

- Go 官方 CodeReviewComments：`https://go.dev/wiki/CodeReviewComments`
- Go 官方 Effective Go：`https://go.dev/doc/effective_go`
- Go 项目布局约定：`https://github.com/golang-standards/project-layout`
- Google Go 风格指南：`https://google.github.io/styleguide/go/decisions`
- Uber Go 风格指南：`https://github.com/uber-go/guide/blob/master/style.md`
